import { JSX } from 'react';
import styles from './all-settings-block.module.scss';

export const FillerBlock: React.FC = ({}): JSX.Element => {
  return <div className={styles.fillerContainer}></div>;
};
